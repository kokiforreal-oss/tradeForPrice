# 外贸询价报价与订单系统 · 产品书（V1.1）

**文档状态：** 待确认  
**版本：** 1.1（按你 2026-08-24 的反馈改写）  
**日期：** 2026-08-24  
**后端：** Python  
**部署：** 阿里云 ECS（Linux）

---

## 1. 产品定位

面向公司内部的 **询价 → 采购报价 → 销售成交 → 订单履约** 系统。客户不登录，由销售录入询价。

**四个功能模块：**

1. 产品库  
2. 询价（含采购报价、销售选用、失败结束）  
3. 订单管理  
4. 问题反馈（每周三汇总发邮件）

**四个角色：** 管理员、销售、采购、财务。权限按模块分配（见第 3 节）。

---

## 2. 业务总流程

```
销售创建询价单（币种默认 RMB）
    → 状态：待报价
    → 采购查看询价单并提交报价
    → 状态：已报价（可有多份报价）
    → 销售选择其中一份报价
    → 状态：销售中
        ├─ 销售失败 → 结束询价单，必填结束原因（如：客户未回复）
        └─ 销售成功 → 自动生成订单（关联该询价单 + 中选报价）
              → 销售填写合同 → 状态：收款
              → 之后每一节点由财务确认：
                 收款 → 工厂生产 → 运输 → 尾款 → 完成
```

**原则：** 一条询价单是一个业务主体。后续报价、选用、成败、订单都挂在这张询价单上。

---

## 3. 角色与权限

| 模块 / 能力 | 管理员 | 销售 | 采购 | 财务 |
|-------------|:------:|:----:|:----:|:----:|
| 用户账号（增删改、分配角色） | ✓ | | | |
| 产品库：查看 | ✓ | ✓ | ✓ | ✓ |
| 产品库：新增 | ✓ | ✓ | ✓ | ✓ |
| 产品库：编辑、删除 | ✓ | | | |
| 询价：新建、编辑自己的待报价单 | ✓ | ✓ | | |
| 询价：查看全部询价 | ✓ | 仅自己创建的 | 待报价/已报价（为报价） | 与订单相关的只读 |
| 报价：对询价单报价、修改未选用的报价 | ✓ | | ✓ | |
| 询价：选择报价，进入「销售中」 | ✓ | ✓ | | |
| 询价：销售失败并结束（填原因） | ✓ | ✓ | | |
| 询价：销售成功（生成订单） | ✓ | ✓ | | |
| 订单：填写合同，从「合同」转到「收款」 | ✓ | ✓ | | |
| 订单：确认收款及后续节点 | ✓ | | | ✓ |
| 订单：查看 | ✓ 全部 | 自己成交的 | | 全部 |
| 问题反馈：提交 | ✓ | ✓ | ✓ | ✓ |
| 问题反馈：查看全部、处理标记 | ✓ | 仅自己提交的 | 仅自己提交的 | 仅自己提交的 |
| 每周三问题汇总邮件 | 系统自动发给指定邮箱 | | | |

管理员可操作所有业务功能，并管理账号。

**数据范围：**

- 销售：只看自己创建的询价单，以及由这些询价单生成的订单。  
- 采购：看所有「待报价 / 已报价」询价单，以便报价；已结束、销售中、已成交的询价可查看但不可再报价。  
- 财务：看全部订单，做节点确认；可新增产品，不报价。

---

## 4. 模块说明

### 4.1 产品库

**谁用：** 全员可新增产品；编辑、删除仅管理员。其他人下拉选用。

**能力：** 列表、搜索、新增；编辑、删除仅管理员。

**删除规则：** 已被询价单引用的产品不允许物理删除，改为「停用」（列表不再可选，历史单据仍显示名称）。未被引用的可删除。

**字段（V1）：**

| 字段 | 说明 |
|------|------|
| 产品分类 | 左侧树维护，产品必选分类更利于检索 |
| 产品ID | 唯一编码，必填 |
| 产品名称 | 必填 |
| 规格 | 选填 |
| 商品类型 | 实物 / 服务 |
| 计价方式 | 固定价 / 移动平均 |
| 计量单位、主计量单位、辅助计量单位、销售单位 | 默认 pcs |
| 参考成本（RMB） | 选填，仅管理员、采购可见 |
| 备注 | 选填 |
| 状态 | 启用 / 停用 |

不做：多语言站、客户自助目录、库存数量。

---

### 4.2 询价功能

**谁用：** 销售创建与推进；采购报价。

#### 询价单（主体）

| 字段 | 说明 |
|------|------|
| 询价单号 | 系统生成，如 `INQ-20260824-0001` |
| 客户名称 | 必填（V1 不做独立客户档案，写在询价单上） |
| 联系人 / 电话 / 邮箱 | 选填 |
| 币种 | 默认 **RMB**，可改为 USD / EUR |
| 需求说明 | 选填 |
| 创建人 | 当前销售 |
| 状态 | 见状态机 |
| 结束原因 | 仅「已结束」时必填 |
| 中选报价 | 选择报价后写入 |

**明细：** 至少一行。每行：产品（从产品库选）、数量、客户目标价（选填）、备注。

#### 询价单状态

| 状态 | 含义 | 谁推进 |
|------|------|--------|
| 待报价 | 刚创建，等采购报价 | 销售创建后自动进入 |
| 已报价 | 至少有一份采购报价 | 采购提交报价后 |
| 销售中 | 销售已选定一份报价，对外谈单 | 销售「选择报价」 |
| 已成交 | 销售成功，已生成订单 | 销售「销售成功」 |
| 已结束 | 销售失败，流程终止 | 销售「结束」并填原因 |

不允许从「已成交 / 已结束」退回。

销售中可：二次询价（回到待报价）、生成订单、或结束。

#### 采购报价

- 销售与采购可 **多轮询价**：采购报价后销售选价进入「销售中」；若价格偏高，销售填写原因发起二次询价，状态回到「待报价」，采购再报。历史报价保留。
- 报价字段：报价说明、交期（天）、明细单价与金额、合计（随询价单币种）。
- 金额 = 数量 × 单价，保留 2 位小数。
- 销售 **选择一份** 报价后进入「销售中」，**此时不自动生成订单**。
- 「销售中 / 已成交 / 已结束」采购不可报价；二次询价后才可再报。
- 谈成后销售点「生成订单」，进入合同填写。未谈成点「结束」并填原因（客户未回复、价格不接受、客户取消、其他）。

#### 销售成功

- 仅「销售中」且已选报价、尚未生成订单时可点「生成订单」。
- 系统用该询价单 + 中选报价 **生成一张订单**；订单完成后询价单变为「已成交」。
- 订单与询价单双向可跳转。

---

### 4.3 订单管理

**谁用：** 销售填合同并转到收款；财务确认后续节点。

**来源：** 销售在询价单点「提交审核」，管理员通过后才进入订单管理。不支持手工空建销售单。

#### 销售订单状态

| 状态 | 谁 | 说明 |
|------|----|------|
| 待审核 | 管理员 | 销售提交后，通过则出现在订单管理；驳回后销售可重提 |
| 待填合同 | 销售 | 按贸易术语（FOB / CIF / EXW 等）填写合同 |
| 履约中 | 采购 + 财务分线 | 采购走采购订单；财务走核销 |
| 完成 | — | 终态 |

提交合同后不再由财务在销售单上点「收款→生产→运输」。物流在采购单上更新；收付款在财务核销中登记。

### 4.3.1 采购订单（独立模块）

采购基于已审核销售单创建采购单 → 管理员审核通过 → **进行中**（可多次更新物流）→ 收货 → 入库 → 验收。

### 4.3.2 财务核销（独立模块）

仅财务、管理员。销售单/采购单审核通过后：

- **对账总览**：销售合同额 vs 累计收款；采购合同额 vs 累计付款；已开票、已核销、余额  
- **收款 / 付款**：多笔流水累计  
- **开票**：销售开票挂销售单，采购收票挂采购单  
- **核销**：发票与收款（或付款）按金额核销，不可超过未核销余额  


#### 订单字段

| 字段 | 说明 |
|------|------|
| 订单号 | 系统生成，如 `ORD-20260824-0001` |
| 关联询价单 / 中选报价 | 只读 |
| 客户信息、币种、产品明细与成交价 | 从询价/报价带入，只读 |
| 合同编号、合同日期、合同备注、合同附件 | 销售在「合同」阶段填写 |
| 首付款金额 / 尾款金额 / 收款备注 | 财务在收款、尾款阶段填写（选填金额，必填确认） |
| 生产备注、运输信息（物流公司、运单号） | 财务在对应阶段选填 |
| 当前流转状态 | 见下表 |

#### 订单流转（严格按序，不可跳步）

| 顺序 | 状态 | 谁操作 | 动作 |
|------|------|--------|------|
| 1 | 合同 | 销售 | 填写合同信息后提交 → 进入「收款」 |
| 2 | 收款 | 财务 | 确认已收首付款 → 进入「工厂生产」 |
| 3 | 工厂生产 | 财务 | 确认生产完成/已下厂 → 进入「运输」 |
| 4 | 运输 | 财务 | 填写物流信息并确认发出 → 进入「尾款」 |
| 5 | 尾款 | 财务 | 确认尾款已收 → 进入「完成」 |
| 6 | 完成 | — | 终态，不可再改 |

每步确认记录：操作人、时间。列表可按状态筛选。不做电子签章、不做在线支付。

---

### 4.4 问题反馈

**谁用：** 所有登录用户。

- 提交：标题、问题描述、截图/附件（选填）、严重程度（一般 / 严重）。  
- 状态：待汇总 / 已发送。  
- **每周三**（服务器时区 **Asia/Shanghai**，建议 09:00）系统把本周「待汇总」的反馈打成一封邮件，发到：

  **wangellen38@gmail.com**

- 邮件主题示例：`[询价系统] 本周问题汇总 YYYY-MM-DD`  
- 正文含：提交人、角色、时间、标题、描述、严重程度。发送成功后这些记录标为「已发送」。  
- 若本周无新问题，仍发一封「本周无新反馈」便于确认任务在跑。  
- 发信依赖服务器 SMTP（部署时配置，见技术节）。管理员可在后台「立即发送一次」便于测试。

---

## 5. 登录与其它约定

- 账号 + 密码登录，中文界面。  
- 预置管理员账号，由管理员给销售/采购/财务开账号。  
- 单据编号按日递增，不复用。  
- 询价单、订单不做物理删除；询价失败用「已结束」。  
- V1 不做：客户门户、PDF 报价导出、汇率自动抓取、钉钉/企微、库存与工厂系统对接、电子合同。

---

## 6. 页面结构

```
登录
└─ 首页（按角色：待办数量）
   ├─ 产品库          （全员可见、可新增；仅管理员可编辑/删除）
   ├─ 询价单          （销售/采购/管理员；财务只读关联单）
   │    └─ 详情：明细 + 报价列表 + 选择报价 / 成功 / 结束
   ├─ 订单管理        （销售自己的成交单；财务全部；管理员全部）
   │    └─ 详情：流转时间轴 + 当前步骤表单
   ├─ 问题反馈
   └─ 用户管理        （仅管理员）
```

---

## 7. 技术与部署（确认后实现）

| 项 | V1 |
|----|-----|
| 后端 | Python 3.12 + FastAPI |
| 前端 | 简易 Web 后台（与 API 一起交付），中文 |
| 数据库 | SQLite（单机 ECS 足够；数据文件定期备份） |
| 认证 | JWT + 角色 |
| 定时任务 | 每周三 09:00 汇总反馈发邮件 |
| 部署 | 阿里云 ECS Linux：systemd 或 Docker Compose + Nginx 反代 |

代码交付时会附：**ECS 上从零安装、启动、开机自启、开放端口、配置 SMTP、备份数据库** 的步骤。

---

## 8. 验收标准

1. 全员可新增产品；管理员可编辑/停用/删除；非管理员不能编辑、删除。  
2. 销售创建询价单，币种默认 RMB；采购能看到并报价。  
3. 销售选中一份报价后，状态为「销售中」；采购不能再报。  
4. 销售失败必须填结束原因，状态为「已结束」，不生成订单。  
5. 销售成功生成订单，且能从订单跳回询价单。  
6. 订单按 合同 → 收款 → 工厂生产 → 运输 → 尾款 → 完成 流转；合同由销售提交，之后由财务确认。  
7. 任意角色可提交问题；每周三（或点「立即发送」）邮件到达 wangellen38@gmail.com。  
8. 在 Ubuntu 的 ECS 上按文档可完成部署并对外访问。

---

## 9. 按你的要求已锁定的内容

- 模块只有 4 个：产品库、询价、订单、问题反馈。  
- 角色只有 4 个：管理员、销售、采购、财务。  
- 询价币种默认 RMB。  
- 询价单是主体；采购报价；销售选报价后进入销售中。  
- 失败由销售结束并填原因。  
- 成交询价关联订单；订单 6 段流转；合同销售填，收款及之后财务确认。   
- 后端 Python，部署目标阿里云 ECS Linux。

---

## 10. 实现时采用的默认（有异议请改）

1. 产品除新增/删除外，管理员可 **编辑**；已引用产品改为 **停用** 而非硬删。  
2. 客户信息不单独建档，写在询价单上。  
3. 同一询价单允许采购提交 **多份报价**，销售只选一份。  
4. 销售只能看自己的询价/订单；采购看全部待办询价；财务看全部订单。  
5. 不做报价 PDF；金额两位小数。  
6. 数据库用 SQLite，方便一台 ECS 部署。  
7. 一并做 Web 后台，不是只交 API。  
8. 反馈邮件：无新问题也发「本周无新反馈」。
