# 拉法国际外贸系统 · 产品书

**文档类型：** 现状说明（与当前代码一致）  
**版本：** 2.0  
**日期：** 2026-08-26  
**形态：** 内部 PC Web 后台（客户不登录）

本文只描述产品能力与流程，不含账号口令、邮箱、服务器地址、密钥等隐私或部署机密。

---

## 1. 产品定位

面向公司内部的外贸协作后台：销售录入询价，采购报价，销售选用报价并提交审核，管理员审核后填写合同，系统生成采购单，财务登记收付款并对账。

**模块：**

1. 工作台  
2. 产品库  
3. 询价单管理  
4. 销售订单  
5. 采购订单  
6. 资金管理（仅管理员、财务）  
7. 用户管理（仅管理员）  
8. 外贸助手  

**角色：** 管理员、销售、采购、财务。

---

## 2. 业务总流程

```
销售创建询价单（币种默认 RMB，可选 USD / EUR）
    → 待报价
    → 全体采购均可查看并报价（可多份、可多轮）
    → 已报价
    → 销售选用其中一份报价
    → 销售中
        ├─ 结束（必填原因）→ 已结束
        ├─ 二次询价（填原因）→ 回到待报价，历史报价保留
        └─ 提交管理员审核 → 生成销售订单（待审核）
              ├─ 驳回：询价单回到销售中，销售可查看驳回原因后重提
              └─ 通过：待填合同
                    → 销售填写贸易术语与合同后提交
                    → 履约中；系统按「中选报价的采购员」生成采购单（待填写）
                    → 仅该采购员可见并填写该采购单
                    → 管理员审核采购单
                    → 进行中（物流更新）→ 收货 → 入库 → 验收 → 完成
                    → 财务在资金管理登记收款 / 付款并对账
```

**原则：** 询价单是报价与选用的主体；销售订单是履约与收款的主体；采购单归属「销售采用的那份报价」的采购员，而不是所有采购共享。

销售也可不经询价、直接新建销售订单草稿并提交审核（无中选报价时，不自动指定采购员）。

---

## 3. 角色与数据范围

| 能力 | 管理员 | 销售 | 采购 | 财务 |
|------|:------:|:----:|:----:|:----:|
| 用户账号增删改、启停、重置密码 | ✓ | | | |
| 产品库查看、新增 | ✓ | ✓ | ✓ | ✓ |
| 产品分类维护；产品编辑、删除/停用 | ✓ | | | |
| 查看参考成本 | ✓ | | ✓ | |
| 新建/编辑自己的待报价询价单 | ✓ | ✓（仅自己） | | |
| 查看询价单 | 全部 | 仅自己创建的 | **全部**（便于报价） | 已生成订单的询价 |
| 对询价单报价 | | | ✓（待报价/已报价） | |
| 选择报价、二次询价、结束、提交审核 | ✓ | ✓（仅自己的单） | | |
| 销售订单查看 | 全部 | 自己创建 / 业务员 / 询价创建人 | 仅中选报价为自己的订单 | 全部 |
| 销售订单审核 | ✓ | | | |
| 填写合同 | ✓ | ✓（自己的单） | | |
| 采购订单查看 | 全部 | 关联到自己销售单的 | **仅 purchaser 为自己的** | 全部 |
| 填写/提交采购单、更新物流、推进收货入库验收 | | | ✓（仅自己的单） | |
| 采购单审核、删除 | ✓ | | | |
| 资金管理（收款/付款/盈利/对账） | ✓ | | | ✓ |
| 外贸助手 | ✓ | ✓ | ✓ | ✓ |

**采购隔离（已实现）：**

- 询价单：所有采购都能看、都能在可报价状态下报价。  
- 采购单：销售采用谁的报价，后续生成的采购单只有该采购能看、能填。  
- 销售订单：采购只能进入「中选报价为自己」的销售单；未中标时询价详情里关联订单号不可点进。

---

## 4. 模块说明

### 4.1 工作台

登录后首页。按角色展示待办数量与待办列表，例如：

- 销售：待报价 / 已报价 / 销售中；待填合同；被驳回需处理的询价。  
- 采购：待报价询价；待填写或已驳回的**自己的**采购单。  
- 财务：待填合同相关统计；待登记的付款单。  
- 管理员：待审销售单、待审采购单等。

### 4.2 产品库

- 左侧分类树（管理员维护编码、名称、层级）。  
- 全员可新增产品；编辑、删除仅管理员。已被单据引用的产品改为停用，不能物理删除。  
- 字段：产品 ID（SKU，唯一）、名称、规格、分类、商品类型（实物/服务）、计价方式（固定价/移动平均）、计量单位 / 主单位 / 辅助单位 / 销售单位、参考成本（仅管理员与采购可见）、备注、启用/停用。  
- 不做：客户自助目录、库存数量、多语言站。

### 4.3 询价单

| 字段 | 说明 |
|------|------|
| 单号 | 系统生成，如 `INQ-年月日-序号` |
| 客户名称 | 必填（不单独建客户档案） |
| 联系人 / 电话 / 邮箱 | 选填 |
| 币种 | 默认 RMB，可 USD / EUR |
| 需求说明 | 选填 |
| 创建人 | 当前销售 |
| 状态 | 待报价 / 已报价 / 销售中 / 已成交(won) / 已结束 |
| 结束原因 | 结束时必填（客户未回复、客户长时间未回、价格不接受、客户取消、其他） |
| 询价轮次 / 二次询价原因 | 多轮时记录 |

明细至少一行：可选产品库或手写品名、规格、单位、数量、目标价、备注。

**报价：** 交期（天）、说明、覆盖全部明细的单价；金额 = 数量 × 单价（两位小数）。报价人记为采购员，供后续采购单归属。

**提交审核：** 销售中且已选报价、尚未生成订单时，提交管理员审核。通过则进入订单管理；驳回后询价可查看原因并重提。

管理员可删除询价单（关联订单一并删除）。

### 4.4 销售订单

状态：草稿（仅销售自建）→ 待审核 → 待填合同 → 履约中 → 完成；另有驳回。

合同阶段必填贸易术语（EXW / FCA / FOB / CFR / CIF / CPT / CIP / DAP / DPU / DDP 等）及对应港口或工厂地址、合同编号与日期、客户国家等。提交合同后进入履约中，并尝试生成采购单。

明细可含税率、赠品、运费、税费、订金等。流转记录可展开查看。

管理员可删除销售订单。

**不再**在销售单上由财务点「收款 → 生产 → 运输 → 尾款」。物流在采购单更新；资金在资金管理登记。

### 4.5 采购订单

- **自动生成：** 销售提交合同后生成，状态为待填写；`purchaser_id` = 中选报价的采购员。  
- **自主新建：** 采购可建不关联或关联「自己中选报价对应销售单」的采购单，业务员为自己。  

状态：待填写 → 待审核 → 进行中 → 收货 → 入库 → 验收 → 完成；可驳回再填。

表头主要字段：单据日期、供应商、开户行、账号、交货时间、发货仓库（整单，明细不填仓库）、币种、运费、税费、订金、项目、联系人/电话、备注。提交审核时交货时间、开户行、账号、明细（品名、采购单位、数量、单价）必填。

进行中可多次登记物流（承运商、运单号），折叠在「进行中」节点内。采购员按序推进收货 / 入库 / 验收 / 完成。

### 4.6 资金管理

仅管理员、财务。四个页签：

| 页签 | 说明 |
|------|------|
| 收款单 | 新建/修改收款凭证，可关联销售订单并核销 |
| 付款单 | 新建/修改付款凭证；采购单审核通过后可自动生成「待填写」付款单 |
| 订单盈利 | 按采购单对照销售金额、已收已付、毛利（折人民币） |
| 对账总览 | 汇总卡片、销售对账表、采购对账表；底部为收付款金额趋势（人民币，按日或按月） |

可按日期区间、销售订单筛选。外币按公开汇率折人民币（失败时用备用汇率）。凭证状态：待填写 `pending` / 已保存 `posted`。

不做在线支付、电子签章。

### 4.7 用户管理

仅管理员：创建用户、分配角色、启停、改姓名、重置密码。

### 4.8 外贸助手

全员：主要城市当地时间与是否工作时间；汇率参考。不替代业务单据。

---

## 5. 登录与其它约定

- 账号 + 密码，中文界面；JWT 会话。  
- 正式环境由管理员开户，禁止使用空库演示口令对外服务。  
- 单据编号按日递增。  
- 客户名、供应商名、对方账号、金额等敏感字段在库内加密存储，浏览器侧按会话密钥解密展示。  
- 入口为站点根路径的单页应用，功能页用哈希路由（如 `#/home`）。  
- **未做：** 客户门户、报价 PDF、钉钉/企微、库存与工厂对接、电子合同、问题反馈周报邮件。

---

## 6. 页面结构

```
登录
└─ 工作台（按角色待办）
   ├─ 产品库
   ├─ 询价单管理
   ├─ 销售订单
   ├─ 采购订单
   ├─ 资金管理        （管理员、财务）
   ├─ 用户管理        （管理员）
   └─ 外贸助手
```

---

## 7. 技术实现（与代码一致）

| 项 | 现状 |
|----|------|
| 后端 | Python 3.9+、FastAPI、SQLAlchemy |
| 前端 | 同仓库静态页（HTML / CSS / JS），无独立前端工程 |
| 数据库 | MySQL 8 社区版；启动时只建缺失表、只给旧表补列，不删数据 |
| 认证 | JWT + 角色 |
| 加密 | 姓名/金额等字段加密；组织密钥文件需长期保留，丢失则历史密文无法解密 |
| 部署 | 本机 `./start.sh`；Linux 可用 systemd + Nginx 反代 |
| 代码布局 | `app/main.py` 入口；`app/core` 权限与加密；`app/db` 模型；`app/api` 接口；`app/static` 前端 |

---

## 8. 验收要点（对照现状）

1. 全员可新增产品；仅管理员可改分类、编辑/停用产品；参考成本仅管理员与采购可见。  
2. 销售只能看到自己的询价单；所有采购能看到全部询价单并在允许状态下报价。  
3. 销售选用某采购的报价并走完合同后，生成的采购单只有该采购能在列表、详情、工作台看到。  
4. 销售失败必须填结束原因，不生成订单。  
5. 销售提交审核，管理员通过后才进入待填合同；驳回原因在询价侧可见。  
6. 合同提交后销售单为履约中；物流在采购单，收付款在资金管理。  
7. 财务/管理员可做收款、付款、盈利、对账；趋势图在对账总览最下方。  
8. 未登录或脚本异常时应能看到登录页；请使用站点根地址访问，不要把业务路径当成独立 HTML。
